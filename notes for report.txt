remember to write about all convergence considerations.

small j added to wavelength for stability

fails at half the wavelength of the grating period (gemini calls this rayleighs anomaly/a type of woods anomaly)

argue for smoothing/no smoothing


Argue that RCWA and other frequency methods are STEADY STATE - ie it automatically captures the infinite geometric series of infinite light passes and so on. This is in contrast to FDTD and so on.

Remember to do the convergence check for num layers with a sinc like function. Right now it is for a grating of height 40 (both comsol and rcwa), but perhaps for a higher grating, it will require more steps. It might be that the convergence criteria is not a number of layers but a max layer height.

Instead of doing convergence for entire curves, also do a check where both order_n and num_layers is checked. Instead of doing it for absorptance curves, just do it for a single structure at a single wavelength, and choose a hard example, for example a sinc function.

Explain the numerical integration scheme, comparing rectangle rule, trapezoidal rule and simpsons rule. Antigravity cli has a conversation "3D utils refactor" detailing some of the math

Explain the skin depth/pec/Ag choice for the conductor, and sanity check of FEM.


Siren activations - look at absorptance curves as a function of effective wavelength. SiREN might learn this implicitly with more layers, minimum of 2.

Fano resonances in absorptance curves

choice of loss - fft for the peaks, mse for the shape. Discuss the ortho norm weigthing and so on. Discuss the implicit assumptions of SIREN. Discuss how this is unavailable in 3d data.

try to generate the data with gradient information. And train the models implicitly with this in mind (sobolev inspired loss) (maybe, this is finicky with torcwa apparently)

Rayleight anomal and instability ( crash at wavelength half of grating_period)

Maybe since the forward models now take the profile as a direct input, it can also output the profile as a direct output. Or maybe the surrogate optimization can optimize this?
